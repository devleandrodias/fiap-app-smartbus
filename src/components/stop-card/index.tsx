import React from 'react';
import {View} from 'react-native';
import {IStopCardProps} from './interfaces/stop-card.interfaces';

export const StopCard: React.FC<IStopCardProps> = () => {
  return <View />;
};
